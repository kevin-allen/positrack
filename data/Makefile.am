iconsdir = $(datadir)
icons_DATA = radar-icon.png
EXTRA_DIST = $(icons_DATA)

## we should try to find a way so that we don't have to set the udev directory ourself
udevrulesdir=/etc/udev/rules.d/
udevrules_DATA = 10-ieee1394.rules

